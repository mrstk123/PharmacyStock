# Environment Configuration for PharmacyStock Backend

# ============================================
# Database Provider Selection
# ============================================
# Choose your database provider: SqlServer or PostgreSQL
# Default: PostgreSQL
DB_PROVIDER=PostgreSQL

# ============================================
# MSSQL (Azure SQL Edge) Configuration
# ============================================
# Use these when DB_PROVIDER=SqlServer
MSSQL_PASSWORD=SamplePass@2026!
MSSQL_CONNECTION=Server=sql-server;Database=PharmacyStockDb;User Id=sa;Password=SamplePass@2026!;TrustServerCertificate=true;

# ============================================
# PostgreSQL Configuration
# ============================================
# Use these when DB_PROVIDER=PostgreSQL
POSTGRES_USER=postgres
POSTGRES_PASSWORD=SamplePass@2026!
POSTGRES_DB=PharmacyStockDb
POSTGRES_CONNECTION=Host=postgres;Database=PharmacyStockDb;Username=postgres;Password=SamplePass@2026!

# ============================================
# Redis Configuration
# ============================================
REDIS_CONNECTION=redis:6379

# ============================================
# API Configuration
# ============================================
ASPNETCORE_ENVIRONMENT=Development
API_PORT=5000
CLIENT_APP_URL=http://localhost:4200

# ============================================
# Usage Instructions
# ============================================
# 
# 1. Copy this file to .env:
#    cp .env.example .env
#
# 2. Choose your database provider by setting DB_PROVIDER
#
# 3. Run with the appropriate profile:
#
#    For MSSQL (Azure SQL Edge):
#      docker-compose --profile mssql up -d
#
#    For PostgreSQL:
#      docker-compose --profile postgres up -d
#
# 4. To switch providers:
#    - Stop current containers: docker-compose down -v
#    - Update DB_PROVIDER in .env
#    - Start with new profile: docker-compose --profile <mssql|postgres> up -d
#
